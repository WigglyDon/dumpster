{
  "name": "dumpster",
  "version": "1.0.0",
  "description": "random code problem dump repository",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --watch src --ext ts --exec ts-node src/FizzBuzz.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.6.2",
    "ts-node": "^10.0.0",
    "nodemon": "^2.0.0"
  }
}
